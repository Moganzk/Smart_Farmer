# ============================================
# Smart Farmer Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env files with real credentials
# ============================================

# ===========================================
# MOBILE APP ENVIRONMENT VARIABLES
# ===========================================
# These are used by the Expo mobile app
# Set via EAS secrets for preview/production builds

# Supabase Configuration (REQUIRED for mobile app)
# Get these from: Supabase Dashboard > Settings > API
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Legacy keys (for backward compatibility)
EXPO_PUBLIC_SUPABASE_KEY=your_anon_key_here

# ===========================================
# SERVER-SIDE / ADMIN ONLY
# ===========================================
# NEVER use these in client code

# Service role key - DANGER: full database access
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Direct database URL (for migrations only)
SUPABASE_DB_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# ===========================================
# OPTIONAL - AI / Future Features
# ===========================================
GEMINI_API_KEY=your_gemini_api_key_here
GROQ_API_KEY=your_groq_api_key_here

# ===========================================
# EAS BUILD INSTRUCTIONS
# ===========================================
# To set secrets for EAS builds:
#
# 1. Preview builds:
#    eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_URL --value "https://your-project.supabase.co" --type string
#    eas secret:create --scope project --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value "your-anon-key" --type string
#
# 2. Production builds:
#    Same commands - EAS secrets apply to all build profiles
#
# 3. Verify secrets:
#    eas secret:list
#
# See: https://docs.expo.dev/build-reference/variables/
